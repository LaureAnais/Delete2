//const comUser = require ('../models/Comment');

//exports.postComment = (req, res, next) => {
  //  comUser.Comment.create({
        // userId req.body.?
        // content req.body.?
   // })
   // .then(() => res.status(201).json({ message: 'commentaire posté !'}))
   // .catch(error => res.status(400).json({ error }))  
// };

// deleteComment => findOne delete
// modifyComment => findOne modify
// admin deleteComment => findOne delete mais vérification de l'id role de l'id en train de supprimer 